#include<bits/stdc++.h>
int main(){
      void rotate(vector<int> &arr) {
          int n=arr.size();
          for( int i=n-1;i>0;i--){
              cout<<arr[i];
          }
          return arr;
      }
  }
